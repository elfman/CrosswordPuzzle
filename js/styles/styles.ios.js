import styles from './styles.common';
import {
  StyleSheet,
} from 'react-native';

styles.gameStyles.topLayout.top = 20;
const gameStyles = StyleSheet.create(styles.gameStyles);
export default {
  gameStyles
};